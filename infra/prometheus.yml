global:
  scrape_interval: 10s

scrape_configs:
  - job_name: codestar
    # Your app needs to expose a /metrics endpoint on this port.
    # Adjust host.docker.internal if running on Linux (use 172.17.0.1 or network_mode: host).
    static_configs:
      - targets: ["host.docker.internal:9100"]
